{ ... }:

{
  # Currently you can't set a default font with home manager.
  # https://github.com/nix-community/home-manager/pull/1268
  # home.packages = with pkgs; [
  #   ibm-plex
  # ];
  # fonts.fontconfig.enable = true;
}
