{ ... }:

{
  services.mako = {
    enable = true;
    anchor = "bottom-left";
    defaultTimeout = 3000;
    # TODO: Merge with Sway style options
    backgroundColor = "#000000";
    borderColor = "#000000";
  };
}
